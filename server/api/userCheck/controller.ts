// import { roomRepository } from "$/repositry/roomRepositry";
// import { defineController } from './$relay';

// export default defineController(() => ({
//   post: async ({ body }) => ({ status: 201, body: await roomRepository.findUser(body.userId) }),
// }));
